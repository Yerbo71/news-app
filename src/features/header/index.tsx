"use client"
import React from 'react';
import {HeaderWrapper} from "@/features/header/styles";

const Header = () => {
    return (
        <HeaderWrapper>

        </HeaderWrapper>
    );
};

export default Header;